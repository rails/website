---
layout: post
title: "This Week in Rails: March 6, 2026"
categories: news
author: Claudio Baccigalupo
og_image: assets/images/this-week-in-rails.png
published: true
date: 2026-03-06
---


Hi, it's [Claudio Baccigalupo](https://github.com/claudiob). Let's explore this week's changes in the Rails codebase.

[Set read-only permissions for GitHub Actions workflow generated by rails new](https://github.com/rails/rails/pull/56913)  
The workflow will run with the minimum required permissions, regardless of the GitHub Organization settings.

[Don't guard action_dispatch_request and action_cable load hooks](https://github.com/rails/rails/pull/56901)  
Removes the load hooks guard for `action_dispatch_request` in Action Pack and `action_cable` in Action Cable.

[Improve Trix support for Action Text markdown](https://github.com/rails/rails/pull/56896)  
Adds `<del>` as a strikethrough alias (Trix emits `<del>`, not `<s>`).
Also, add a `visit_div` handler so Trix-style `<div>` blocks pass through their children's content, letting `<br>` tags handle paragraph spacing naturally.

[Specify PostgreSQL 9.5 or higher is required](https://github.com/rails/rails/pull/56911)  
Rails includes `array_position` which is not available on previous versions.

[Fix Encoding::CompatibilityError with non-ASCII strict locals defaults](https://github.com/rails/rails/pull/56906)  
Fixes an error raised by a template loaded via `File.binread` using strict locals with non-ASCII default values (e.g. `<%# locals: (label: "café") -%>`).

[Fix IsolatedExecutionState.share_with call](https://github.com/rails/rails/pull/56902)  
When `isolation_level = :fiber`, it's wrong to pass the original thread to `share_with`.

[Fix collection caching to preserve store default expires_in](https://github.com/rails/rails/pull/56891)  
This change ensures that `expires_in` is only included in the arguments to `write_multi` when it is present in the collection cache options.


_You can view the whole list of changes [here](https://github.com/rails/rails/compare/@%7B2026-02-27%7D...main@%7B2026-03-06%7D)._  
_We had [12 contributors](https://contributors.rubyonrails.org/contributors/in-time-window/20260227-20260306) to the Rails codebase this past week!_

Until next time!  

_[Subscribe](https://world.hey.com/this.week.in.rails) to get these updates mailed to you._
